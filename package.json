{
  "name": "react-click-to-source",
  "version": "1.0.0",
  "description": "Chrome extension — click React components to open them in Cursor",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^3.0.0"
  }
}
